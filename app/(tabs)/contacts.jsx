import * as Clipboard from 'expo-clipboard';
import * as Contacts from 'expo-contacts';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { Alert, FlatList, Pressable, RefreshControl, Text, View } from 'react-native';
import { Card, Field, Header, colors, styles } from '@/components/SurveyUI';
export default function ContactsScreen(){const [items,setItems]=useState([]);const [query,setQuery]=useState('');const [refreshing,setRefreshing]=useState(false);const load=useCallback(async()=>{setRefreshing(true);const permission=await Contacts.requestPermissionsAsync();if(permission.granted){const result=await Contacts.getContactsAsync({fields:[Contacts.Fields.PhoneNumbers]});setItems(result.data);}else Alert.alert('Permission required','Contacts access is needed.');setRefreshing(false)},[]);useEffect(()=>{load()},[load]);const list=useMemo(()=>items.filter(c=>`${c.name} ${c.phoneNumbers?.[0]?.number||''}`.toLowerCase().includes(query.toLowerCase())),[items,query]);return <View style={styles.screen}><Header title="Contacts" subtitle={`${items.length} contacts available`}/><Field label="Search contacts" placeholder="Name or number" value={query} onChangeText={setQuery}/><FlatList data={list} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={load}/>} ListEmptyComponent={<Text style={{textAlign:'center',color:colors.muted,marginTop:30}}>No contacts found.</Text>} keyExtractor={(item)=>item.id} renderItem={({item})=>{const number=item.phoneNumbers?.[0]?.number;return <Card><View style={{flexDirection:'row',alignItems:'center'}}><View style={{width:42,height:42,borderRadius:21,backgroundColor:colors.pale,alignItems:'center',justifyContent:'center'}}><Text style={{fontWeight:'800',color:colors.primary}}>{(item.name||'?')[0].toUpperCase()}</Text></View><View style={{marginLeft:12,flex:1}}><Text style={{fontWeight:'800',color:colors.ink}}>{item.name||'Unnamed contact'}</Text><Text style={{color:colors.muted,marginTop:4}}>{number||'No Number'}</Text></View>{number&&<Pressable onPress={()=>{Clipboard.setStringAsync(number);Alert.alert('Copied','Contact number copied.')}}><Text style={{color:colors.primary,fontWeight:'800'}}>Copy</Text></Pressable>}</View></Card>}}/></View>}
